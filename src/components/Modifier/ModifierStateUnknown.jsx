import React from 'react';

import {
	modifierStateUnknown,
} from './styles';

const ModifierStateUnknown = (props) => (
	<div
		className={ modifierStateUnknown }
		{ ...props }
	/>
);

export default ModifierStateUnknown;
