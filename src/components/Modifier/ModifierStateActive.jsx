import React from 'react';

import {
	modifierStateActive,
} from './styles';

const ModifierStateActive = (props) => (
	<div
		className={ modifierStateActive }
		{ ...props }
	/>
);

export default ModifierStateActive;
