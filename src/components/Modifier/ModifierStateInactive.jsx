import React from 'react';

import {
	modifierStateInactive,
} from './styles';

const ModifierStateInactive = (props) => (
	<div
		className={ modifierStateInactive }
		{ ...props }
	/>
);

export default ModifierStateInactive;
